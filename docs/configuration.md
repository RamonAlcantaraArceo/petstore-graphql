# Configuration

Configuration is centralized in `src/app/config.py` with defaults that can be overridden
later via environment variables.

Current values exposed by `health.details`:
- `version`
- `build_date`
- `git_commit_sha`
